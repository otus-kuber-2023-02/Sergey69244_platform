apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ name }}-pvc"
spec:
  storageClassName: manual
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: {{ storage_size }}
